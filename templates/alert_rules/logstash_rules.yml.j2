#jinja2: trim_blocks: True, lstrip_blocks: True
{% if logstash_pic is defined and logstash_pic|length %}
# {{ ansible_managed }}

{%   if logstash_rules is defined and logstash_rules|length %}
groups:
{{ logstash_rules | to_nice_yaml | indent(2, true) }}
{%   endif %}
{% endif %}